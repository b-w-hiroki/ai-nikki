# AI Nikki — スマート日記アシスタント 開発プロンプト

> このドキュメントは Cursor AI に渡して開発を進めるためのプロジェクト仕様書兼プロンプトです。
> Phase ごとに段階的に実装してください。

---

## 🎯 プロジェクト概要

**AI Nikki** は、日めくりカレンダーを起点とした AI 搭載の日記アプリケーションです。

### コンセプト
「今日をめくる → 書く → 過去の自分と比べる」

日めくりカレンダーをめくる行為がアプリの起点。1日1ページの気軽さで書き続けると、いつの間にか自分だけの記録集が完成する。過去の自分との比較を通じて成長を実感できる仕組み。

### ターゲット
- 一般公開（GitHub Pages でホスティング）
- スマートフォンメイン、PC でも快適に使える

### 開発者のバックグラウンド
- ゲームクリエイター出身
- 触り心地・マイクロインタラクション・ゲーミフィケーションを重視
- 既存プロジェクト（osanpo-bingo 等）と同様の技術スタックで統一

---

## 🛠 技術スタック

既存プロジェクト（osanpo-bingo, nomisuke-bingo, bingo-party 等）に合わせた構成：

| 項目 | 技術 |
|------|------|
| フロントエンド | HTML5 + CSS3 + JavaScript (ES6+) バニラ構成 |
| AI 連携 | Anthropic Claude API（ユーザーが設定画面で自身の API キーを入力する方式） |
| データ保存 | localStorage（日記データ、設定、実績すべて） |
| データバックアップ | JSON エクスポート / インポート機能 |
| ホスティング | GitHub Pages |
| PWA | Service Worker + manifest.json でオフライン対応 |
| フォント | Zen Maru Gothic（見出し）、Noto Sans JP（本文）、Klee One（手書き風テキスト） |

### フォント読み込み
```html
<link href="https://fonts.googleapis.com/css2?family=Zen+Maru+Gothic:wght@400;500;700&family=Noto+Sans+JP:wght@400;500;700&family=Klee+One:wght@400;600&display=swap" rel="stylesheet">
```

---

## 🎨 デザインシステム

### カラーパレット（CSS 変数）

```css
:root {
  /* ベース */
  --bg: #f7f3ee;
  --bg-card: #fffdf9;
  --bg-warm: #faf6f0;
  
  /* テキスト */
  --text-primary: #2c2418;
  --text-secondary: #8a7e6d;
  --text-hint: #b5a997;
  
  /* アクセント */
  --accent: #e07a3a;
  --accent-light: #fdebd4;
  --accent-dark: #b85a1f;
  
  /* 気分カラー */
  --mood-great: #4CAF50;
  --mood-good: #8BC34A;
  --mood-ok: #FFC107;
  --mood-bad: #FF9800;
  --mood-awful: #f44336;
  
  /* ゲーミフィケーション */
  --streak-fire: #ff6d00;
  --streak-glow: #ffab40;
  --xp-bar: #7c5cbf;
  --xp-bg: #e8e0f0;
  
  /* シャドウ */
  --shadow-soft: 0 2px 12px rgba(44, 36, 24, 0.06);
  --shadow-card: 0 4px 20px rgba(44, 36, 24, 0.08);
  --shadow-float: 0 8px 32px rgba(44, 36, 24, 0.12);
  
  /* 角丸 */
  --radius: 14px;
  --radius-sm: 10px;
  --radius-lg: 20px;
  
  /* フォント */
  --font-display: 'Zen Maru Gothic', sans-serif;
  --font-body: 'Noto Sans JP', sans-serif;
  --font-handwrite: 'Klee One', cursive;
}
```

### テーマ・スキン（Phase 4 で実装）

5 種類のテーマを切り替え可能にする：

| テーマ名 | 特徴 |
|----------|------|
| ペーパー | デフォルト。温かみのあるクリーム系 |
| ミッドナイト | ダークテーマ。#1a1a2e ベース |
| レトロ | セピア調。#f0e6d3 ベース |
| フォレスト | グリーン系。#e8f5e9 ベース |
| さくら | ピンク系。#fce4ec ベース |

### デザイン原則
- 紙・ノートのような温かみのあるテクスチャ感
- 罫線入りテキストエリア（手書きノート風）
- カレンダー穴のディテール（スキューモーフィズム）
- すべてのインタラクションに心地よいアニメーション
- モバイルファースト（max-width: 420px をメインコンテナ）

---

## 📱 画面構成（5 画面 + ボトムナビ）

### ボトムナビゲーション
```
[ 📝 日記 ] [ 📅 カレンダー ] [ 💬 AIチャット ] [ 👤 マイページ ]
```

### Screen 1: 日めくりホーム（メイン画面）

**概要**: アプリの起点。日めくりカレンダーをめくって今日の記録を書く。

**構成要素**（上から順に）:

1. **ヘッダー**: アプリ名「AI Nikki」+ カレンダー / プロフィールアイコン
2. **ストリーク＆経験値バー**: 連続日数、EXP バー、レベルバッジ
3. **日めくりカレンダーカード**:
   - 上部: オレンジグラデーションヘッダーに年月日＋曜日
   - カレンダー穴（2つ）の装飾
   - 気分スタンプ（5段階: 最高/いい感じ/ふつう/ちょっと…/つらい）
   - テキストエリア（罫線付きノート風、Klee One フォント）
   - アクションボタン行: 写真追加 / 積み上げトグル
   - 写真プレビュー（添付時）
   - 積み上げトラッカー（トグル表示）
   - 保存ボタン
4. **タイムトラベルカード**: 1ヶ月前/1年前の日記がある場合に自動表示

**日めくりアニメーション**: アプリ起動時に `rotateX(-90deg)` → `rotateX(0)` のフリップアニメーション（0.7s, cubic-bezier）

### Screen 2: 月間カレンダー

**概要**: 月表示のカレンダー。書いた日を気分カラーのドットで可視化。

**構成要素**:
- 月ナビ（前月/次月ボタン）
- 曜日ヘッダー（日〜土）
- 日付グリッド（7列）
  - 日記がある日は気分カラーのドット表示
  - 今日の日付はアクセントカラーで強調
  - タップで過去の日記を閲覧
- 「先月」「去年の同月」へのワンタップ切替

### Screen 3: タイムトラベル（Phase 2）

**概要**: 過去の自分と今の自分を比較する画面。

**表示パターン**:
| 期間 | 用途 |
|------|------|
| 1週間前 | 先週の自分をチラ見。軽い振り返り |
| 1ヶ月前 | 当時の目標を AI が検出し進捗を問いかけ |
| 1年前 | 大きな変化を AI が分析して可視化 |

**AI 比較機能**: 過去と現在の日記を Claude API に送り、「成長ポイント」「変化」「続けられていること/止まっていること」を分析。

### Screen 4: AI 深堀りチャット（Phase 3）

**概要**: 日記に対して AI とチャット形式で対話する。

**機能**:
- 日記内容に基づく AI の問いかけ（「なぜそう感じた？」「次はどうしたい？」）
- 過去の日記コンテキストも参照した横断的な気づき
- チャット UI（吹き出し形式）

**API 呼び出し例**:
```javascript
const response = await fetch('https://api.anthropic.com/v1/messages', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'x-api-key': userApiKey,           // ユーザーが設定画面で入力した API キー
    'anthropic-version': '2023-06-01',
    'anthropic-dangerous-direct-browser-access': 'true'
  },
  body: JSON.stringify({
    model: 'claude-sonnet-4-20250514',
    max_tokens: 1024,
    system: `あなたは優しく思慮深い日記カウンセラーです。ユーザーの日記を読み、以下の観点でフィードバックしてください：
    - 感情の深堀り（なぜそう感じたか）
    - ポジティブな気づき
    - 過去の記録との関連性
    - 次のアクションの提案
    日本語で簡潔に応答してください。`,
    messages: conversationHistory
  })
});
```

### Screen 5: マイページ / プロフィール（Phase 4）

**概要**: ユーザーのレベル、実績、統計、設定を管理。

**構成要素**:
- プロフィールヘッダー（アバター、レベル、称号）
- 統計カード（総日記数、総文字数、写真数）
- 今月の積み上げサマリー（プログレスバー付き）
- 実績バッジ一覧
- 感情コレクション（Phase 4）
- テーマ切替（Phase 4）
- API キー設定
- データエクスポート / インポート

---

## 🎮 ゲーミフィケーション設計

### 1. 連続記録ストリーク
- 毎日書くと連続日数がカウントアップ
- 炎アイコンが脈動アニメーション（`scale(1) → scale(1.15)`, 1.5s ループ）
- マイルストーン演出:
  - 7 日: 炎が大きくなる
  - 30 日: 金色の炎に変化
  - 100 日: 特別演出（紙吹雪）
  - 365 日: 伝説の炎

### 2. ライターレベル & 経験値
- EXP 獲得条件:
  - 日記を保存: +50 EXP
  - 写真を添付: +10 EXP
  - 気分スタンプ選択: +5 EXP
  - 積み上げ記録: +10 EXP（カテゴリごと）
  - 500 文字以上書く: +20 EXP ボーナス
- レベルアップ必要 EXP: `レベル × 200`（Lv.1→200, Lv.2→400...）
- 称号（レベルに連動）:
  | レベル | 称号 |
  |--------|------|
  | 1-4 | かけだしライター |
  | 5-9 | 見習いライター |
  | 10-19 | 一人前ライター |
  | 20-29 | ベテランライター |
  | 30-49 | マスターライター |
  | 50+ | 伝説のライター |

### 3. 実績バッジ
| バッジ名 | 条件 | アイコン |
|----------|------|----------|
| 初投稿 | 初めて日記を書く | ✏️ |
| 7日連続 | 7日間連続記録 | 🔥 |
| 写真家 | 写真を5枚添付 | 📷 |
| 30日連続 | 30日間連続記録 | 🏆 |
| 1万文字 | 累計1万文字達成 | 📚 |
| 夜更かし | 深夜0時以降に投稿 | 🌙 |
| 早起き | 朝6時前に投稿 | 🌅 |
| 多感 | 5種類すべての気分を記録 | 🎭 |
| 読書家 | 読書累計1000ページ | 📖 |
| アスリート | 運動累計1000分 | 💪 |
| 100日連続 | 100日間連続記録 | ⭐ |
| 年間制覇 | 365日間記録（連続でなくてOK） | 👑 |

**バッジ解放演出**: モーダルポップアップ（`scale(0.5) → scale(1.05) → scale(1)`, 0.6s）

### 4. 感情コレクション（Phase 4）
- 記録した感情パターンが図鑑に登録される
- レアな組み合わせ（例: 同日に「つらい」→「最高」）で特別なコレクション

---

## 📊 積み上げトラッカー

### デフォルトカテゴリ
| カテゴリ | アイコン色 | 単位 |
|----------|-----------|------|
| 読書 | #5c9ce6（青） | ページ / 冊 |
| 運動 | #4CAF50（緑） | 分 / km |
| 勉強 | #7c5cbf（紫） | 時間 |

### カスタムカテゴリ
- ユーザーが自由に追加可能
- カテゴリ名、アイコン文字（1文字）、色、単位を設定

### データ構造
```javascript
// localStorage: 'ai-nikki-accumulation'
{
  "2026-03-31": {
    "reading": { "value": 30, "unit": "pages" },
    "exercise": { "value": 45, "unit": "min" },
    "study": { "value": 2, "unit": "hours" }
  }
}
```

---

## 🗓 Year in Review（Phase 5）

Spotify Wrapped のようにスライド形式で1年を振り返る特別画面。12月に自動解放、途中経過も任意に確認可能。

### スライド構成（6 枚）

**Slide 1: 数字で見る1年**
- 総日記数、総文字数、写真数、最長連続日数
- カウントアップアニメーション（0 から最終値まで 2 秒）

**Slide 2: 積み上げサマリー**
- カテゴリごとの年間合計を棒グラフ表示
- 月別推移の折れ線グラフ

**Slide 3: 感情の旅路**
- 12ヶ月の感情推移グラフ
- 最もポジティブだった月をハイライト
- 転換点になった日をマーク

**Slide 4: AI が選ぶベスト日記**
- AI が1年の中から「最も心が動いた日」「最も成長を感じた日」「最も面白かった日」をピックアップ
- Claude API で選定理由も生成

**Slide 5: 成長レポート**
- 年初と年末の自分を AI が比較分析
- 「今年のあなたはこう変わった」
- 目標達成度の可視化

**Slide 6: 来年の自分へ**
- AI が今年の傾向から来年へのアドバイスを提案
- ユーザーが来年の目標を書き込み可能

### 演出
- スワイプで次スライドへ（パララックス効果付き）
- 最終スライドで紙吹雪エフェクト
- 各スライドを SNS 用画像として保存可能（html2canvas）

---

## 💾 データ設計

### localStorage キー一覧
| キー | 内容 |
|------|------|
| `ai-nikki-entries` | 日記エントリ（全日分） |
| `ai-nikki-accumulation` | 積み上げデータ |
| `ai-nikki-settings` | 設定（APIキー、テーマ、カスタムカテゴリ） |
| `ai-nikki-stats` | 統計（レベル、EXP、ストリーク、バッジ） |
| `ai-nikki-photos` | 写真データ（Base64、圧縮済み） |

### 日記エントリ構造
```javascript
// localStorage: 'ai-nikki-entries'
{
  "2026-03-31": {
    "date": "2026-03-31",
    "mood": "great",           // great | good | ok | bad | awful
    "text": "今日は...",
    "photos": ["data:image/jpeg;base64,..."],  // 圧縮済み、最大800px
    "accumulation": {
      "reading": { "value": 30, "unit": "pages" },
      "exercise": { "value": 45, "unit": "min" }
    },
    "aiAnalysis": {            // AI 分析結果（Phase 2）
      "emotion": { "positive": 0.8, "negative": 0.1, "neutral": 0.1 },
      "keywords": ["プロジェクト", "達成感"],
      "insight": "..."
    },
    "createdAt": "2026-03-31T22:30:00+09:00",
    "wordCount": 342
  }
}
```

### 統計データ構造
```javascript
// localStorage: 'ai-nikki-stats'
{
  "level": 5,
  "exp": 650,
  "expToNext": 1000,
  "streak": {
    "current": 7,
    "longest": 14,
    "lastDate": "2026-03-31"
  },
  "badges": ["first-entry", "streak-7", "photographer"],
  "totalEntries": 23,
  "totalWords": 12450,
  "totalPhotos": 8
}
```

---

## ✨ マイクロインタラクション仕様

### 保存時
- リップルエフェクト（ボタン中央から白い円が広がる）
- ボタンテキストが「保存しました！」に変化（2 秒間）
- ボタン色がオレンジ → グリーンに変化
- トースト通知「✏️ +50 EXP!」が上部からスライドイン

### 気分スタンプ選択時
- 選択したスタンプが `scale(1.1)` + オレンジのボーダー
- リングエフェクト（外側に広がって消える波紋）

### ストリーク
- 炎アイコンが常時脈動（`scale(1) ↔ scale(1.15)`, brightness 変化）

### レベルアップ時
- EXP バーがスムーズに伸びる（`transition: width 0.8s ease`）
- レベルアップ時はトースト + バッジ解放演出と同様のモーダル

### ホバー / タップ
- カード系: `translateY(-1px)` + シャドウ強化
- ボタン系: `scale(1.08)` on hover, `scale(0.95)` on active

### 日めくりフリップ
- ページ読み込み 300ms 後に開始
- `rotateX(-90deg)` から `rotateX(0)` へ
- `cubic-bezier(0.23, 1, 0.32, 1)` イージング、700ms

---

## 📂 ファイル構成

```
ai-nikki/
├── index.html              # ランディングページ
├── app.html                # メインアプリ（SPA）
├── css/
│   ├── styles.css          # メインスタイル
│   ├── animations.css      # アニメーション定義
│   └── themes/
│       ├── paper.css       # デフォルトテーマ
│       ├── midnight.css    # ダークテーマ
│       ├── retro.css       # レトロテーマ
│       ├── forest.css      # フォレストテーマ
│       └── sakura.css      # さくらテーマ
├── js/
│   ├── app.js              # メインアプリロジック、画面遷移
│   ├── diary.js            # 日記の保存・読込・表示
│   ├── calendar.js         # カレンダー表示・月切替
│   ├── mood.js             # 気分スタンプ管理
│   ├── photo.js            # 写真の添付・圧縮・プレビュー
│   ├── accumulation.js     # 積み上げトラッカー
│   ├── gamification.js     # レベル・EXP・ストリーク・バッジ
│   ├── timetravel.js       # タイムトラベル（過去比較）
│   ├── ai-chat.js          # AI チャット機能
│   ├── ai-analysis.js      # AI 感情分析・レポート生成
│   ├── year-review.js      # Year in Review
│   ├── theme.js            # テーマ切替
│   ├── storage.js          # localStorage ヘルパー
│   └── export.js           # エクスポート・インポート
├── assets/
│   ├── icons/              # アプリアイコン
│   └── sounds/             # SE ファイル（将来用）
├── manifest.json           # PWA マニフェスト
├── service-worker.js       # Service Worker
├── ACTION_PLAN.md          # 開発アクションプラン
├── BASIC_SPEC.md           # 基本仕様書
├── CHANGELOG.md            # 変更履歴
└── README.md               # プロジェクト説明
```

---

## 🚀 開発ロードマップ

### Phase 1: 日めくりホーム（まずここを完成させる）
- [ ] index.html（ランディングページ）
- [ ] app.html（メインアプリ骨格）
- [ ] 日めくりカレンダーカード（フリップアニメーション付き）
- [ ] 気分スタンプ（5 段階、選択アニメーション）
- [ ] テキストエリア（罫線付きノート風）
- [ ] 写真添付（カメラ撮影 / ギャラリー、自動圧縮 max 800px）
- [ ] 積み上げトラッカー入力（トグル表示、3 カテゴリ + カスタム）
- [ ] 保存機能（localStorage）
- [ ] ストリーク表示（炎アニメーション）
- [ ] EXP バー + レベルバッジ表示
- [ ] 保存時のマイクロインタラクション（リップル、トースト、+EXP）
- [ ] ボトムナビゲーション（画面切替の骨格）
- [ ] レスポンシブ対応

### Phase 2: カレンダー + タイムトラベル + AI 感情分析
- [ ] 月間カレンダー UI
- [ ] 日付セルの気分カラードット
- [ ] 過去の日記閲覧
- [ ] 先月 / 去年の同月切替
- [ ] タイムトラベルカード（1 週間前 / 1 ヶ月前 / 1 年前）
- [ ] API キー設定画面
- [ ] Anthropic Claude API 連携
- [ ] 日記保存時の感情分析（API 呼び出し）
- [ ] 過去比較の AI 分析

### Phase 3: AI 深堀りチャット + 週次レポート
- [ ] チャット UI（吹き出し形式）
- [ ] AI との対話機能（会話履歴管理）
- [ ] 過去の日記コンテキスト参照
- [ ] 週次レポート自動生成（感情推移、頻出キーワード、AI 要約）

### Phase 4: ゲーミフィケーション + テーマ + PWA
- [ ] レベルアップ演出
- [ ] バッジ解放システム + モーダル演出
- [ ] 感情コレクション図鑑
- [ ] テーマ切替（5 テーマ）
- [ ] PWA 対応（manifest.json, Service Worker）
- [ ] データエクスポート / インポート

### Phase 5: 積み上げサマリー + Year in Review
- [ ] 月次積み上げサマリー
- [ ] Year in Review（6 スライド）
- [ ] カウントアップアニメーション
- [ ] スワイプ遷移 + パララックス
- [ ] 紙吹雪エフェクト
- [ ] SNS 共有用画像生成

---

## 🖼 UI モック参照

Phase 1 の UI モックが `ai-nikki-phase1-mock.html` として存在します。
このファイルのデザイン・レイアウト・カラー・アニメーションを忠実に再現してください。

モックの主な特徴:
- 日めくりカレンダーカードが中央に大きく表示
- オレンジグラデーションのカレンダーヘッダー
- カレンダー穴の装飾（スキューモーフィズム）
- 罫線付きテキストエリア
- ストリーク炎 + EXP バーが上部に常時表示
- 「1ヶ月前の今日」カードが下部に表示
- ボトムナビで画面切替

---

## ⚠ 実装上の注意事項

1. **API キーの扱い**: ユーザーが設定画面で入力する方式。localStorage に保存。コードにハードコードしない。フロントエンドから直接 API を呼ぶため `anthropic-dangerous-direct-browser-access: true` ヘッダーが必要。

2. **写真の圧縮**: Canvas API で最大 800px にリサイズ、JPEG quality 0.7 で圧縮してから Base64 で localStorage に保存。

3. **ストリーク判定**: 毎日0時にリセット判定。前日に記録があれば継続、なければリセット。タイムゾーンは `Intl.DateTimeFormat` で端末のローカル時間を使用。

4. **パフォーマンス**: localStorage の容量制限（通常 5-10MB）を意識。写真が増えると圧迫するため、古い写真の圧縮率を上げるか、エクスポート促進のトーストを表示。

5. **既存プロジェクトとの一貫性**: osanpo-bingo と同様に README.md, ACTION_PLAN.md, BASIC_SPEC.md, CHANGELOG.md を整備する。

6. **Cursor での実行方法**: `npx --yes serve -l 3000` でローカルサーバー起動。

---

## 📝 実装開始の指示

Phase 1 から順に実装してください。
まず以下を作成してください：

1. `README.md` — プロジェクト概要（osanpo-bingo の README を参考に）
2. `app.html` — メインアプリ。上記 UI モックのデザインを忠実に再現しつつ、js/ と css/ に分離した構成で実装
3. `css/styles.css` — デザインシステムの CSS 変数 + 全スタイル
4. `css/animations.css` — すべてのアニメーション定義
5. `js/app.js` — 画面遷移、初期化
6. `js/diary.js` — 日記の CRUD
7. `js/mood.js` — 気分スタンプ
8. `js/photo.js` — 写真機能
9. `js/accumulation.js` — 積み上げトラッカー
10. `js/gamification.js` — レベル、EXP、ストリーク、バッジ
11. `js/storage.js` — localStorage ヘルパー
12. `manifest.json` + `service-worker.js` — PWA 基盤

マイクロインタラクションとアニメーションは最初から組み込んでください。「触り心地の良さ」がこのアプリの差別化ポイントです。
